<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kg.seeot.mybatis.board.BoardMapper">
<resultMap type="com.kg.seeot.board.dto.BoardDTO" 
											id="board">
<id property="memberId" column="member_id"/>
<result property="boardNo" column="board_no"/>
<result property="boardGroup" column="board_group"/>
<result property="boardIndent" column="board_indent"/>
	<result property="boardTitle" column="board_title"/>
	<result property="boardContent" column="board_content"/>
	<result property="boardDate" column="board_date"/>
	<result property="boardFile" column="board_file"/>
	<result property="memberName" column="member_name"/>
	
</resultMap>
<select id="boardList" resultMap="board">
select * from seeot_board
<!-- 
	select B.* from(select rownum rn, A.* from
	(select * from seeot_board order by board_no desc)A)B
	 						where rn between #{s} and #{e}
 -->
</select> 

<select id="boardContentView" resultMap="board">
select * from seeot_board where member_id=#{memberId}
</select>

<delete id="delete">
	delete from seeot_board where member_id=#{memberId}
</delete>


<insert id="writeSave" >
    insert into seeot_board(board_no,member_id,board_title,board_content,board_file)
    values(board_seq.nextval,#{memberId},#{boardTitle},#{boardContent},#{boardFile})
</insert>



</mapper>

















